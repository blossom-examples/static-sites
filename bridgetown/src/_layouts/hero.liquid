<!doctype html>
<html lang="{{ site.locale }}">
  <head>
    {% render "head", metadata: site.metadata, title: data.title %}
  </head>
  <body class="marketing {{ data.page_class }}">
    <main>
      {{ content }}
    </main>
  </body>
</html>